variables:
  GRADLE_OPTS: "-Dorg.gradle.daemon=false"
  
stages:
 - build
 
build-phase:      
  stage: build
  tags:
   - pms
  before_script:
    - id
    - pwd
  script:
    - echo "run build"
    - npm run build